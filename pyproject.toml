[build-system]
requires = ["setuptools>=69"]
build-backend = "setuptools.build_meta"

[project]
name = "football-predictor"
version = "0.1.0"
description = "Leakage-safe Over 2.5 football probability model"
requires-python = ">=3.11"
